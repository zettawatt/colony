import {
    GridBox
} from "std-widgets.slint";

export component ColoniesView inherits GridBox {
    Row {
        Rectangle {
            colspan: 4;
            vertical_stretch: 1;
            horizontal_stretch: 1;
        }
    }
}
